const express = require('express'); const sqlite3 = require('sqlite3').verbose(); const app = express(); const db = new sqlite3.Database('/tmp/challenge.db'); const flag = process.env.FLAG || 'vladilk{local-double-query}';
db.serialize(() => { db.run('CREATE TABLE IF NOT EXISTS users (id INTEGER, name TEXT)'); db.run('CREATE TABLE IF NOT EXISTS secret_flag (flag TEXT)'); db.run('DELETE FROM secret_flag'); db.run('INSERT INTO secret_flag VALUES (?)', flag); db.run("INSERT INTO users VALUES (1, 'alice')"); });
app.get('/', (req,res) => { const id=req.query.id||'1'; const sql=`SELECT id,name FROM users WHERE id='${id}' AND (SELECT CASE WHEN (SELECT count(*) FROM secret_flag)>0 THEN 1 ELSE 0 END)=1`; db.all(sql,(err,rows)=>res.send(`<h1>Double-query SQLi</h1><p>Query: <code>${esc(sql)}</code></p>${err?`<pre>SQLite error: ${esc(err.message)}</pre>`:`<pre>${esc(JSON.stringify(rows))}</pre>`}`)); });
function esc(v){return String(v).replace(/[&<>]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;'}[c]));} app.listen(80);
